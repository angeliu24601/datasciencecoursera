{
    "collab_server" : "",
    "contents" : "setwd(\"./hw\")\n\n#4 If I execute the expression x <- 4 in R, what is the class of the object `x' as determined \n#by the `class()' function?\nx<-4\nclass(x)\n\n#5 What is the class of the object defined by x <- c(4, \"a\" ,TRUE)?\nx <- c(4, \"a\", TRUE)\nclass(x)\n\n#6 If I have two vectors x <- c(1,3, 5) and y <- c(3, 2, 10), what is produced by the expressio\n#n cbind(x, y)?\nx <- c(1,3, 5); y <- c(3, 2, 10); ans <- rbind(x, y)\nlength(ans); dim(ans); class(ans)\n\n#8 Suppose I have a list defined as x <- list(2, \"a\", \"b\", TRUE). What does x[[2]] give me? \n#Select all that apply.\nx <- list(2, \"a\", \"b\", TRUE)\nx[[2]]; class(x[[2]]); length(x[[2]])\n\n#9 Suppose I have a vector x <- 1:4 and y <- 2. What is produced by the expression x + y?\nx <- 1:4; y <- 2; x + y; class(x + y)\n\n#10 Suppose I have a vector x <- c(3, 5, 1, 10, 12, 6) and I want to set all elements of this \n#vector that are less than 6 to be equal to zero. What R code achieves this? Select all that \n#apply.\nx <- c(3, 5, 1, 10, 12, 6)\nx[x < 6] == 0 # no\nx[x> 6] <- 0 # no\nx[x<6]==0 # no\nx[x==6] <- 0 # no\nx[x<=5] <-0 # yes\nx[x>0] <-6 # no\nx[x != 6] <- 0 # no\nx[x == 0] <- 6 # no\nx[x %in% 1:5] <- 0 # yes\nx[x < 6] <- 0 # yes\nx[x > 6] <- 0 # no\n#Use the Week 1 Quiz Data Set to answer questions 11-20.\nhw1 <- read.csv(\"./hw1_data.csv\")\n\n#11 what are the column names of the dataset?\ncolnames(hw1)\n\n#12 Extract the first 2 rows of the data frame and print them to the console. What does the output #look like?\nhead(hw1,2)\n\n#13 How many observations (i.e. rows) are in this data frame?\nnrow(hw1)\n\n#14 Extract the last 2 rows of the data frame and print them to the console. What does the output \n#look like?\ntail(hw1,2)\n\n#15 What is the value of Ozone in the 47th row?\nhw1$Ozone[47]\n\n#16 How many missing values are in the Ozone column of this data frame?\nsum(is.na(hw1$Ozone))\n\n#17 What is the mean of the Ozone column in this dataset? Exclude missing values (coded as NA) \n#fromthis calculation.\nmean(hw1$Ozone, na.rm = TRUE)\n\n#18 Extract the subset of rows of the data frame where Ozone values are above 31 and Temp \n#values #are above 90. What is the mean of Solar.R in this subset?\nmean(hw1[hw1$Ozone > 31 & hw1$Temp >90,]$Solar.R, na.rm = TRUE)\n\n#19 #What is the mean of \"Temp\" when \"Month\" is equal to 6?\nmean(hw1[hw1$Month == 6,]$Temp, na.rm = TRUE)\n\n#20 What was the maximum ozone value in the month of May (i.e. Month is equal to 5)?\nmax(hw1[hw1$Month == 5,]$Ozone, na.rm = TRUE)\n",
    "created" : 1461580341792.000,
    "dirty" : false,
    "encoding" : "UTF-8",
    "folds" : "",
    "hash" : "221719098",
    "id" : "9287437B",
    "lastKnownWriteTime" : 1461580943,
    "last_content_update" : 1461580943980,
    "path" : "~/Downloads/OneDrive - Inter-American Development Bank Group/Coursera_DataScience/02_R-Programming/assingments/hw/02_r-programming_hw1_script.R",
    "project_path" : "hw/02_r-programming_hw1_script.R",
    "properties" : {
    },
    "relative_order" : 2,
    "source_on_save" : false,
    "source_window" : "",
    "type" : "r_source"
}